import type { ReactNode } from 'react';
import { InvalidCredentialPage } from '@/components/pages/invalid-credential-page';

export default function InvalidCredential(): ReactNode {
  return <InvalidCredentialPage />;
}
